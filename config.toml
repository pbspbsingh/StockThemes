chrome_path = "/usr/bin/google-chrome"
user_data_dir = "/home/pbs/Workspace/ChromeData"
chrome_args = []
launch_chrome_if_needed = true

log_config = "info,chromiumoxide=error,stock_themes=debug"

base_ticker = "QQQ"
market_hours = ["06:30", "13:00"]
ignored_stocks = []

http_port = 8000

[trade_analysis]
daily_chart_days = 365
daily_chart_post_days = 15
hourly_chart_days = 30
hourly_chart_post_days = 5
